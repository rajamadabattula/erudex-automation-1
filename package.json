{
  "name": "erudex-automation",
  "version": "1.0.0",
  "description": ".",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "Cypress": "npx cypress open"
  },
  "devDependencies": {
    "cypress": "^6.2.0",
    "cypress-file-upload": "^5.0.2"
  }
}